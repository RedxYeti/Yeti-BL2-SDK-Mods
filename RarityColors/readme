example usage
from ..RarityColors.Rarity_Colors import GetRarityColorHex

ItemRarity = MyWeapon.GetRarityLevel()
ItemRarityColor = GetRarityColorHex(ItemRarity)
item_name = MyWeapon.GetInventoryCardString(False, True, True)

title = "this weapon is:"
message = f"<font size='20' color='{ItemRarityColor}'>{item_name}</font>"
PC = GetEngine().GamePlayers[0].Actor
HUDMovie = PC.GetHudMovie()
if HUDMovie is not None:
    HUDMovie.ClearTrainingText()
    HUDMovie.AddTrainingText(message, title, training_text_time, (), "", False, 0, PC.PlayerReplicationInfo, True)

this will output a hud training message with the title "this weapon is" and message will be the name of the weapon with the font color of the rarity.
